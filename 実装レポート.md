# 東京農工大学将棋部公式サイト 実装レポート

- 作成日: 2026-08-03
- 最終更新: 2026-08-04
- 対象要件: `university_shogi_site_requirements.md`
- 詳細設計: `university_shogi_site_detailed_design.md`
- 実装方式: React + Tailwind CSS + Vite

---

## 1. 実装概要

大学将棋部公式サイトを、ReactコンポーネントとTailwind CSSを使用した和モダンなデザインへ刷新した。

トップページにはヒーローセクションとAboutセクションを実装し、既存のエンブレム、ロゴ、ナビゲーション、連絡先を維持した共通ヘッダー／フッターをReact化した。また、既存URLが404にならないよう、入部案内、大会記録、活動紹介も同じReactレイアウト上で表示できる構成にした。

Viteの開発・プレビューサーバーだけでなく、GitHub Pagesや`python3 -m http.server`のような通常の静的Webサーバーでも動作するよう、ビルド済みHTML、JavaScript、CSS、画像をプロジェクト直下へ自動配置する仕組みを追加した。

## 2. 実装した画面

| URL | 画面 | 主な内容 |
|---|---|---|
| `index.html` | トップ | Hero、About、共通Header／Footer |
| `top.html` | 互換転送 | canonicalとmeta refreshにより`/`へ転送 |
| `entry.html` | 入部案内 | 新入生向け案内、キャンパスマップ、活動内容、入部・見学方法 |
| `record.html` | 大会記録 | 年度別の大会成績 |
| `introduce.html` | 活動紹介 | 日頃の活動、大会・部内戦、活動写真 |

すべての画面で共通ヘッダーとフッターを表示する。

## 3. コンポーネント構成

```text
App
├── Header
├── PageContent
│   ├── HeroSection
│   ├── AboutSection
│   │   └── FeatureCard × 3
│   └── ContentPage
│       ├── EntryPage
│       ├── RecordPage
│       └── IntroducePage
└── Footer
```

### 3.1 Header

- `Designer.png`のエンブレムと`logo.png`のロゴを表示
- 画面上部へ固定
- PCではナビゲーションを横並びで表示
- スマートフォンではMENUボタンを表示
- Reactの`useState`でモバイルメニューを開閉
- Escapeキー、リンク選択、PC幅への変更時にメニューを閉じる
- `aria-expanded`、`aria-controls`、`aria-hidden`を設定
- Xリンクは新しいタブで開く

### 3.2 HeroSection

- キャッチコピー「詰みは見える。」「将来は見えない。」を2行で表示
- キャッチコピーは画面幅に応じて32〜72pxの範囲で変化し、各文の途中では改行しない
- 説明文「経験を問わず、将棋を楽しみたい仲間を歓迎します。」「将棋部です。将棋だけしているとは言っていません。」を表示
- 東京農工大学将棋部公式サイトであることを明記
- CSSによる9×9の将棋盤と駒型の抽象ビジュアルを実装
- 盤面上の駒には大学名を表す「農」「工」を表示
- 駒内の文字は横50%、縦58%の光学中心へ配置
- 盤面脇に「農工大で、王手より先に単位をかける。」を縦書きで表示
- 写真に依存しないファーストビューを構成
- PCではテキストと盤面を2列表示
- スマートフォンでは縦並び表示

CTAは次の2件を実装した。

| CTA | 動作 |
|---|---|
| XのDMで見学を相談する | `https://x.com/tuatshogiclub`を新しいタブで開く |
| 活動紹介を見る | `introduce.html`へ移動する |

見学CTA付近には「見学をご希望の方は、X（旧Twitter）のDMでその旨をお送りください。」と表示し、予約フォームやメール予約への誘導は設けていない。

### 3.3 AboutSection

以下の3枚のカードをデータ配列から`map`で描画する。

1. 初心者歓迎
2. 柔軟な活動頻度
3. 充実の対局環境

PCでは3列、タブレットでは2列、スマートフォンでは1列になる。カードには控えめな影、枠線、将棋を連想させる装飾文字、ホバー時の浮き上がりを設定した。

### 3.4 Footer

- トップ、入部案内、大会記録、活動紹介へのリンク
- 公式Xへのリンク
- `tuatshogi@gmail.com`へのメールリンク
- `© 東京農工大学将棋部`
- PCではナビゲーションを横並び、スマートフォンでは縦並びで表示

### 3.5 既存ページ

既存のページURLを維持するため、入部案内、大会記録、活動紹介をReactコンポーネントとして実装した。ビルド時にページ定義から`App`へページIDを渡して静的HTMLを生成し、ブラウザでは同じページIDでhydrateする。

### 3.6 入部案内のキャンパスマップ

- 「活動場所」セクションへ小金井キャンパスマップを表示
- 元画像は`cumpasmap.jpg`として管理し、Viteビルド時にハッシュ付き画像として`assets/`へ出力
- altは「小金井キャンパス内のサークル棟B棟までの案内図」
- 赤線で示したサークル棟B棟に部室があることを説明
- 東京農工大学公式の小金井キャンパスマップを出典リンクとして表示
- 出典リンクは`target="_blank"`と`rel="noopener noreferrer"`を指定
- スマートフォンではコンテンツ幅に合わせて縮小し、横方向へはみ出さない

### 3.7 大会記録の更新

大会結果は`src/components/pages/ContentPage.jsx`内の`records`配列で管理する。年度ごとの`items`へ大会結果を追加し、`highlight: true`を指定した結果は金色のラベルで強調する。

更新作業の詳細は`tournament_result_update_guide.md`に記載している。

## 4. デザイン実装

### 4.1 カラー

| 用途 | 色 |
|---|---|
| 墨色 | `#0F172A` |
| 紺色 | `#0F3350` |
| 背景 | `#F8FAF8` |
| カード背景 | `#FFFFFF` |
| 本文 | `#272A2A` |
| 罫線 | `#D9DEDA` |
| 金色アクセント | `#B5974C` |

### 4.2 フォント

- 見出し: Shippori Mincho、Noto Serif JP
- 本文・ナビゲーション: Noto Sans JP
- Webフォント読込失敗時はserif／sans-serifへフォールバック

### 4.3 装飾

- CSSグラデーションによる薄い格子背景
- 9×9の将棋盤
- `clip-path`を使用した駒型
- 駒内の「農」「工」を専用要素で中央配置
- カード内の低透明度装飾文字
- 活動写真の色紙風フレームと影
- キャンパスマップのカード型フレームと影

## 5. レスポンシブ対応

| 画面幅 | 主な表示 |
|---|---|
| 767px以下 | モバイルメニュー、Hero縦並び、CTA縦並び、カード1列、Footerナビ縦並び |
| 768〜1023px | PCナビ、Aboutカード2列 |
| 1024px以上 | Hero 2列、Aboutカード3列 |

- 最小幅320pxを想定
- Header固定分の余白を`main`へ設定
- PC・スマートフォンで横方向のはみ出しがないことを確認済み
- CTAやメニューボタンは44px以上の操作領域を確保

## 6. アクセシビリティ対応

- `header`、`nav`、`main`、`section`、`footer`を使用
- h1、h2、h3の見出し階層を整理
- 装飾要素へ`aria-hidden="true"`を設定
- 外部リンクが新しいタブで開くことをスクリーンリーダー向けに補足
- モバイルメニューへ`aria-expanded`と`aria-controls`を設定
- Escapeキーによるメニュー終了とMENUボタンへのフォーカス復帰
- キーボードフォーカスリングを表示
- `prefers-reduced-motion`に対応
- 画像に内容を表すaltを設定
- キャンパスマップの出典を`figcaption`と外部リンクで明示

## 7. SEO対応

- 日本語ページとして`lang="ja"`を設定
- 4ページの本文をビルド時にHTMLへプリレンダリング
- ページごとに固有のtitle、meta description、canonicalを設定
- 全ページへOGPとTwitter Cardを設定
- トップページへOrganization形式のJSON-LDを設定
- `sitemap.xml`と`robots.txt`をビルド時に生成
- `top.html`を正規URL`/`への転送専用ページへ変更
- 既存のGoogle Search Console確認用メタタグを維持

## 8. データ管理

`src/data/siteConfig.js`で次の情報を一元管理する。

- 大学名、部名
- エンブレムとロゴ
- 連絡先メールアドレス
- 公式X URL

URL、title、description、canonicalは`src/data/pageDefinitions.js`で一元管理する。
- CTAリンク
- ナビゲーション項目
- Aboutカードデータ

表示内容やURLを変更する場合は、原則としてこのファイルを更新する。

ページ固有データは`src/components/pages/ContentPage.jsx`で管理する。

- 入部案内のセクション情報
- キャンパスマップの表示有無
- 年度別の大会結果
- 活動紹介の写真と説明文

大会結果の追加・修正方法は`tournament_result_update_guide.md`を参照する。

## 9. ビルド・静的配信対応

### 9.1 開発環境

```bash
npm install
npm run dev
```

### 9.2 本番ビルド

```bash
npm run build
```

`npm run build`では次を自動実行する。

1. `templates/`の開発用HTMLをプロジェクト直下へ復元
2. `sharp`でOGP画像と構造化データ用ロゴを生成
3. Reactの4ページをHTMLへプリレンダリング
4. ViteでReact、JSX、Tailwind CSSをビルド
5. `sitemap.xml`と`robots.txt`を生成
6. プロジェクト直下の古い`assets/`を削除
7. 配信用HTML、SEOファイル、OGP画像、`dist/assets/`をプロジェクト直下へコピー

ビルド後のプロジェクト直下は、Viteなしの静的サーバーから配信できる。

```bash
python3 -m http.server
```

### 9.3 HTMLテンプレート

`index.html`などの直下HTMLはビルド時に生成・更新される。開発用の元HTMLは`templates/`に保管しているため、直接HTMLを修正する場合は`templates/`側を変更する。

## 10. ファイル構成

```text
mycraft/
├── index.html                 # 静的配信用トップ
├── top.html                  # 静的配信用トップ互換URL
├── entry.html                # 静的配信用入部案内
├── record.html               # 静的配信用大会記録
├── introduce.html            # 静的配信用活動紹介
├── assets/                   # ビルド済みJS、CSS、画像
├── cumpasmap.jpg             # キャンパスマップのビルド元画像
├── Designer.png              # エンブレムのビルド元画像
├── logo.png                  # ロゴのビルド元画像
├── 20260709_180604.jpg       # 日頃の活動写真
├── 20260524_191148.jpg       # 大会参加時の写真
├── src/
│   ├── App.jsx
│   ├── main.jsx
│   ├── index.css
│   ├── components/
│   │   ├── home/
│   │   ├── layout/
│   │   └── pages/
│   └── data/siteConfig.js
├── templates/                # ビルド前HTMLテンプレート
├── scripts/
│   ├── restore-templates.mjs
│   └── deploy-root.mjs
├── package.json
├── package-lock.json
├── vite.config.js
├── tailwind.config.js
├── postcss.config.js
├── university_shogi_site_requirements.md
├── university_shogi_site_detailed_design.md
├── implementation_report.md
└── tournament_result_update_guide.md
```

## 11. 不具合修正履歴

### 11.1 内部リンクの404

初回実装ではReactの入口が`index.html`のみで、`entry.html`、`record.html`、`introduce.html`が存在しなかった。そのため、各HTMLをViteのマルチページ入力として追加し、URLごとに対応するReactコンポーネントを表示するよう修正した。

### 11.2 通常の静的サーバーで空画面になる問題

当初のHTMLは`/src/main.jsx`を直接参照していたため、Viteを介さない静的サーバーではブラウザがJSX、Reactのbare import、CSS importを処理できず、空画面になっていた。

対策として、ビルド済みJavaScript、CSS、画像をプロジェクト直下から相対パスで読み込む静的配信構成へ変更した。これにより、単純なHTTPサーバーでもReact画面が描画される。

### 11.3 Heroのキャッチコピーと駒文字

Heroのキャッチコピーを「詰みは見える。」「将来は見えない。」へ変更し、画面幅に応じた文字サイズと明示的な2行構成を設定した。

盤面上の駒文字を「農」「工」へ変更した。文字は専用の`.shogi-piece__label`で配置し、フォントの行高によるずれを除去した。駒本体と一緒に回転した状態でも、駒型の光学中心へ収まるよう調整した。

### 11.4 入部案内へのキャンパスマップ追加

バックアップに保存されていた`cumpasmap.jpg`を復元し、入部案内の「活動場所」へ追加した。地図画像、サークル棟B棟の案内、大学公式マップへの出典リンクを実装した。

## 12. 検証結果

### 12.1 ビルド

- `npm run build`: 成功
- ViteによるJSX変換: 成功
- Tailwind CSS生成: 成功
- 静的配信用ファイル配置: 成功

### 12.2 ページ応答

以下はすべてHTTP 200を確認した。

- `/`
- `/index.html`
- `/top.html`
- `/entry.html`
- `/record.html`
- `/introduce.html`
- ビルド済みJavaScript
- ビルド済みCSS
- エンブレム、ロゴ、活動写真、キャンパスマップ

### 12.3 ブラウザ描画

ヘッドレスChromeと通常の静的HTTPサーバーを使用して確認した。

- React rootの描画成功
- Hero、About、Header、Footerの表示成功
- XのDM CTAと補足文の表示成功
- 入部案内、大会記録、活動紹介の表示成功
- 入部案内のキャンパスマップ、説明文、出典リンクの表示成功
- 「農」「工」の駒文字が駒型の中央へ配置されることを確認
- PC 1440px幅で表示崩れなし
- モバイル390px幅で表示崩れ・横方向のはみ出しなし
- モバイルMENU表示とCTA縦並びを確認

上記の検証はメイン作業とサブエージェントによる独立プレビューの双方で合格した。

## 13. ファイル整理とバックアップ

現行React構成から参照されない旧JavaScript、旧CSS、旧Markdown、旧設計書、Zone.Identifier、再生成可能な`node_modules/`と`dist/`を削除した。キャンパスマップ画像は入部案内で再利用するため、バックアップから復元して現行構成へ組み込んだ。

削除前のプロジェクト全体は次のファイルへバックアップしている。

```text
/home/tomokane/.ssh/web/mycraft_backup_20260803_2245.tar.gz
```

- バックアップサイズ: 約42MB
- tarアーカイブの読み取り確認済み
- キャンパスマップ復元後のプロジェクト容量: 約24MB

## 14. 今後の作業候補

- キャンパスマップ元画像のファイル名`cumpasmap.jpg`を`campus-map.jpg`へ統一
- Lighthouseによる本番環境での性能計測
- 大会記録や活動情報を更新しやすくするデータファイル分離
- CIによる`npm run build`と静的配信ファイル更新の自動化

---

以上。
