# 大会結果 更新手順書

- 対象サイト: 東京農工大学将棋部公式サイト
- 対象画面: `record.html`（大会記録）
- 更新元: `src/components/pages/ContentPage.jsx`
- 最終更新: 2026-08-04

---

## 1. 概要

大会結果は、次のファイル内にある`records`配列で管理している。

```text
src/components/pages/ContentPage.jsx
```

大会結果を追加・修正した後は、`npm run build`を実行して配信用の`record.html`と`assets/`を更新する。

## 2. 作業前の準備

プロジェクトディレクトリへ移動する。

```bash
cd /home/tomokane/.ssh/web/mycraft
```

`node_modules`がない場合は、依存関係をインストールする。

```bash
npm install
```

`package-lock.json`に記録されたバージョンを厳密に使用する場合は、次のコマンドでもよい。

```bash
npm ci
```

## 3. 大会結果データの構造

`src/data/records.js`内のデータは、次の形式になっている。年度は配列へ書いた順番ではなく、`publishedAt`の新しい順に自動表示される。

```js
const records = [
  {
    year: "2026年度（令和8年度）",
    sourceUrl: "http://kantoshogi.web.fc2.com/kekka/R08/R08kekka.html",
    publishedAt: "2026-08-10T00:00:00Z",
    sortOrder: 0,
    items: [
      {
        date: "2026.05.24",
        event: "春季団体戦C2級",
        result: "2位・昇級",
        detail: "6勝1敗　C1級へ昇級",
        highlight: true,
      },
    ],
  },
];
```

各項目の意味は次のとおり。

| 項目 | 必須 | 内容 | 記入例 |
|---|---:|---|---|
| `year` | 必須 | 西暦と元号を併記した年度見出し | `"2026年度（令和8年度）"` |
| `sourceUrl` | 必須 | 関東大学将棋連盟の該当年度結果ページ | `"http://kantoshogi.web.fc2.com/kekka/R08/R08kekka.html"` |
| `publishedAt` | 必須 | 公開日時。CMSでは公開操作時に自動設定 | `"2026-08-10T00:00:00Z"` |
| `sortOrder` | 必須 | 公開日時が同じ場合の表示順。小さい値が先 | `0` |
| `date` | 必須 | 大会開催日 | `"2026.05.24"` |
| `event` | 必須 | 大会名、部門、クラス | `"春季団体戦C2級"` |
| `result` | 必須 | 順位と昇降級などの要約 | `"2位・昇級"` |
| `detail` | 必須 | 勝敗、昇降級・残留の補足 | `"6勝1敗　C1級へ昇級"` |
| `highlight` | 任意 | 結果ラベルを金色で強調する | `true` |

## 4. 同じ年度へ結果を追加する

対象年度の`items`配列へ、新しい大会結果を追加する。

新しい結果を上に追加すると、画面でも新しい順に表示される。

```js
{
  year: "2026年度（令和8年度）",
  sourceUrl: "http://kantoshogi.web.fc2.com/kekka/R08/R08kekka.html",
  publishedAt: "2026-08-10T00:00:00Z",
  sortOrder: 0,
  items: [
    {
      date: "2026.10.18",
      event: "秋季団体戦C1級",
      result: "3位",
      detail: "4勝3敗　C1級残留",
    },
    {
      date: "2026.05.24",
      event: "春季団体戦C2級",
      result: "2位・昇級",
      detail: "6勝1敗　C1級へ昇級",
      highlight: true,
    },
  ],
},
```

注意事項:

- 各大会結果の末尾にはカンマを付ける。
- 日付は既存データに合わせて`YYYY.MM.DD`形式にする。
- 新しい結果から古い結果の順に並べる。
- 大会名・所属級・順位・勝敗・昇降級は、`sourceUrl`の公式結果と照合する。
- JavaScriptの引用符や括弧を削除しない。

## 5. 新しい年度を追加する

新年度のデータを`records`の元データへ追加する。記載位置に関係なく、`publishedAt`の新しい年度から表示される。

```js
const records = [
  {
    year: "2027年度（令和9年度）",
    sourceUrl: "公式結果ページのURL",
    publishedAt: "2027-05-23T00:00:00+09:00",
    sortOrder: 0,
    items: [
      {
        date: "2027.05.23",
        event: "春季団体戦",
        result: "優勝・昇級",
        detail: "全勝　○級へ昇級",
        highlight: true,
      },
    ],
  },
  {
    year: "2026年度（令和8年度）",
    sourceUrl: "http://kantoshogi.web.fc2.com/kekka/R08/R08kekka.html",
    publishedAt: "2026-08-10T00:00:00Z",
    sortOrder: 0,
    items: [
      // 既存の2026年度データ
    ],
  },
];
```

年度の公式結果ページが公開されたことを確認し、そのURLを`sourceUrl`へ設定する。表示順は`publishedAt`の新しい順で自動決定され、同時刻の場合だけ`sortOrder`の小さい値が先になる。画面では各年度の「公式結果を見る」リンクとして表示される。

## 6. 結果を強調表示する

優勝、準優勝、昇級など、特に強調したい結果には`highlight: true`を追加する。

```js
{
  date: "2027.05.23",
  event: "春季団体戦",
  result: "優勝・昇級",
  detail: "全勝　○級へ昇級",
  highlight: true,
}
```

表示仕様:

- `highlight: true`あり: 金色の結果ラベル
- `highlight`なし: 淡い紺色の結果ラベル

通常結果では`highlight`を記述する必要はない。

## 7. 既存結果を修正する

誤字や結果の訂正は、対象データの値を直接変更する。

変更前:

```js
result: "3位",
detail: "4勝3敗　C1級残留",
```

変更後:

```js
result: "2位",
detail: "5勝2敗　B2級残留",
```

日付、大会名、結果、詳細を公式記録と照合してから変更する。

## 8. 配信用ファイルを更新する

データの編集後、次のコマンドを実行する。

```bash
npm run build
```

この処理では次の作業が自動実行される。

1. HTMLテンプレートを復元する。
2. ReactとTailwind CSSをビルドする。
3. `dist/`へビルド結果を出力する。
4. 配信用HTMLと`assets/`をプロジェクト直下へ配置する。
5. 古いハッシュ付きJS／CSSを削除し、新しいファイルへ置き換える。

コマンドの最後に次のメッセージが表示されれば、配信用ファイルの配置は成功している。

```text
Static site deployed to the project root.
```

## 9. ローカルで確認する

プロジェクト直下で静的サーバーを起動する。

```bash
python3 -m http.server
```

ブラウザで次のURLを開く。

```text
http://localhost:8000/record.html
```

確認後は、ターミナルで`Ctrl+C`を押してサーバーを終了する。

## 10. 確認項目

次の項目を確認する。

- 新しい年度が既存年度より上に表示されている。
- 同年度内で新しい大会が上に表示されている。
- 日付が`YYYY.MM.DD`形式になっている。
- 大会名、クラス、結果、勝敗数に誤りがない。
- 各年度の「公式結果を見る」が正しい年度ページを新しいタブで開く。
- 強調対象だけが金色のラベルになっている。
- PC表示で文字やカードが重なっていない。
- スマートフォン表示で横方向にはみ出していない。
- ヘッダーとフッターの「大会記録」から移動できる。
- ブラウザを再読み込みしても結果が表示される。

## 11. Gitへ反映する

変更内容を確認する。

```bash
git status --short
git diff -- src/components/pages/ContentPage.jsx
```

ソースとビルド済み配信ファイルをステージする。

```bash
git add \
  src/components/pages/ContentPage.jsx \
  index.html \
  top.html \
  entry.html \
  record.html \
  introduce.html \
  assets
```

コミット例:

```bash
git commit -m "大会結果を追加"
```

`npm run build`を実行すると、`assets/`内のハッシュ付きJavaScriptやCSSのファイル名が変更される場合がある。古いファイルの削除と新しいファイルの追加も一緒にコミットする。

## 12. 更新作業の簡易チェックリスト

```text
[ ] src/data/records.jsの大会記録データを更新した
[ ] 新しい年度・大会が上になるよう並べた
[ ] 日付、大会名、結果、詳細を確認した
[ ] yearに西暦と元号を記載し、sourceUrlに公式結果ページを設定した
[ ] 勝敗、昇降級を公式結果と照合した
[ ] 必要な結果にhighlight: trueを付けた
[ ] npm run buildが成功した
[ ] record.htmlをブラウザで確認した
[ ] PCとスマートフォンの表示を確認した
[ ] ソースと配信用ファイルをGitへ追加した
```

---

以上。
