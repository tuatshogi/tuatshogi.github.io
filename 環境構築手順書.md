# 開発・実行環境 構築手順書

- 対象: 東京農工大学将棋部 公式Webサイト
- リポジトリ: `git@github.com:tuatshogi/tuatshogi.github.io.git`
- 作業ディレクトリ: `/home/tomokane/.ssh/web/mycraft`
- 最終確認日: 2026-08-06

---

## 1. 目的

本手順書は、現在のPCで当サイトを開発、ビルド、テストできる環境を再構築するための手順をまとめたものである。

現在のプロジェクトはWindows上のWSL2で実行する。依存パッケージは`package-lock.json`で固定し、通常は`npm ci`で導入する。

## 2. 現在の実行環境

2026-08-06時点で動作確認した環境は次のとおり。

| 項目 | 現在の環境 | 備考 |
|---|---|---|
| 実行基盤 | WSL2 | Windows Subsystem for Linux 2 |
| Linux | Ubuntu 24.04.4 LTS | x86_64 |
| Node.js | 20.20.2 | 必要条件は20.19.0以上 |
| npm | 10.8.2 | Node.jsに同梱 |
| Git | 2.43.0 | GitHubとSSH接続 |
| Python | 3.12.3 | ローカル静的サーバーで使用 |
| Google Chrome | 150.0.7871.186 | Lighthouseで使用 |
| Playwright | 1.62.1 | `package-lock.json`で固定 |
| Lighthouse CI | 0.15.1 | `package-lock.json`で固定 |

バージョンは次のコマンドで確認できる。

```bash
cat /etc/os-release
node --version
npm --version
git --version
python3 --version
google-chrome --version
```

## 3. WSL2とUbuntuの準備

### 3.1 現在のPCをそのまま使う場合

WSL2とUbuntu 24.04は導入済みのため、Windows TerminalからUbuntuを開く。Windows PowerShellで次を実行し、`VERSION` が `2` であることを確認する。

```powershell
wsl --list --verbose
```

WSL自体を更新する場合は次を実行する。

```powershell
wsl --update
```

### 3.2 WSL2がないPCに再構築する場合

管理者権限のWindows PowerShellで次を実行し、必要に応じてWindowsを再起動する。

```powershell
wsl --install -d Ubuntu-24.04
```

初回起動時にUbuntu用のユーザー名とパスワードを設定する。以降のコマンドはUbuntuのターミナルで実行する。

### 3.3 Ubuntuの基本パッケージ

```bash
sudo apt update
sudo apt install -y ca-certificates curl gnupg git python3 build-essential
```

プロジェクトは`/mnt/c/...`ではなく、現在と同じUbuntu側の`/home/...`下に配置する。これにより、npmの依存導入やビルド時のファイルアクセスを安定させられる。

## 4. Node.js 20の導入

まず現在のバージョンを確認する。`v20.19.0`以上が表示される場合、この章の導入作業は不要である。

```bash
node --version
npm --version
```

Node.jsがない、またはバージョンが古い場合は、現在のPCと同じNodeSourceのNode.js 20系リポジトリを設定する。

```bash
curl -fsSL https://deb.nodesource.com/gpgkey/nodesource-repo.gpg.key \
  -o /tmp/nodesource-repo.gpg.key
sudo gpg --dearmor --yes \
  -o /usr/share/keyrings/nodesource.gpg \
  /tmp/nodesource-repo.gpg.key
sudo chmod a+r /usr/share/keyrings/nodesource.gpg
```

`/etc/apt/sources.list.d/nodesource.sources`を作成する。

```bash
sudo tee /etc/apt/sources.list.d/nodesource.sources >/dev/null <<'EOF'
Types: deb
URIs: https://deb.nodesource.com/node_20.x
Suites: nodistro
Components: main
Architectures: amd64
Signed-By: /usr/share/keyrings/nodesource.gpg
EOF
```

Node.jsを導入する。

```bash
sudo apt update
sudo apt install -y nodejs
node --version
npm --version
```

`node --version`が`v20.19.0`以上であることを確認する。プロジェクトの`npm`コマンドに`sudo`は付けない。

## 5. GitとGitHub SSH接続の準備

### 5.1 Gitの基本設定

現在のPCでは設定済みである。未設定のPCだけ、自分の情報に置き換えて実行する。

```bash
git config --global user.name "GitHubの表示名"
git config --global user.email "GitHubに登録したメールアドレス"
```

### 5.2 SSH接続の確認

```bash
ssh -T git@github.com
```

GitHubのユーザー名と「successfully authenticated」が表示されれば接続できている。GitHubはSSHシェルを提供しないため、この確認コマンドの終了コードが`1`でも、認証成功メッセージが出ていれば問題ない。

`Permission denied (publickey)`になる場合はSSH鍵を作成する。すでに`~/.ssh/id_ed25519`がある場合は上書きせず、既存鍵を使用する。

```bash
ssh-keygen -t ed25519 -C "GitHubに登録したメールアドレス"
cat ~/.ssh/id_ed25519.pub
```

表示された公開鍵だけをGitHubの`Settings` → `SSH and GPG keys` → `New SSH key`へ登録する。秘密鍵`~/.ssh/id_ed25519`は共有しない。

## 6. リポジトリと依存パッケージの準備

### 6.1 現在のPCで開く

```bash
cd /home/tomokane/.ssh/web/mycraft
git status --short --branch
git remote -v
```

`origin`が次のURLを指していることを確認する。

```text
git@github.com:tuatshogi/tuatshogi.github.io.git
```

### 6.2 リポジトリがない場合だけ取得する

```bash
mkdir -p /home/tomokane/.ssh/web
git clone \
  git@github.com:tuatshogi/tuatshogi.github.io.git \
  /home/tomokane/.ssh/web/mycraft
cd /home/tomokane/.ssh/web/mycraft
```

別PCに構築する場合は、`/home/tomokane`をそのPCのUbuntuユーザーのホームディレクトリに読み替える。

### 6.3 npm依存パッケージを導入する

```bash
npm ci
```

`npm ci`は`package-lock.json`に記録されたバージョンを厳密に導入する。`node_modules/`はGit管理対象外であり、別PCへコピーせず、各環境で`npm ci`を実行する。

導入結果を確認する。

```bash
npm list --depth=0
npx vite --version
npx playwright --version
```

## 7. 開発サーバーの起動

```bash
cd /home/tomokane/.ssh/web/mycraft
npm run dev
```

ターミナルに表示されたURLをWindows側のブラウザで開く。通常は次のURLになる。

```text
http://localhost:5173/
```

終了するときは、起動したターミナルで`Ctrl+C`を押す。

Windows側から接続できない場合は、WSL外部からも受信できる形で起動する。

```bash
npm run dev -- --host 0.0.0.0
```

## 8. ビルドと通常検査

### 8.1 本番ビルド

```bash
npm run build
```

ビルドは画像最適化、ReactのHTMLプリレンダリング、Viteビルド、SEOファイル生成を行う。`dist/`に加え、GitHub Pages配信用のHTMLと`assets/`をプロジェクト直下へ配置する。

次のメッセージが出れば配置成功である。

```text
Static site deployed to the project root.
```

### 8.2 推奨の一括検査

```bash
npm test
```

`npm test`で次を順番に実行する。

1. 本番ビルド
2. canonical、OGP、構造化データなどのSEO検査
3. HTMLと参照アセットの成果物検査
4. HTML構文検査

2026-08-06に現在のPCで実行し、すべて成功することを確認済みである。

### 8.3 ビルド後の静的サイトを確認する

```bash
python3 -m http.server 8000 --bind 127.0.0.1
```

Windows側のブラウザで`http://localhost:8000/`を開く。確認後は`Ctrl+C`で終了する。

## 9. Playwright視覚・操作テスト

初回だけ、Playwright用ChromiumとOS依存パッケージを導入する。

```bash
npx playwright install --with-deps chromium
```

現在のPCに導入済みのブラウザは次で確認できる。

```bash
npx playwright install --list
```

視覚・操作テストを実行する。

```bash
npm run test:visual
```

テスト中は設定ファイルが`127.0.0.1:4173`に一時サーバーを自動起動する。2026-08-06に17件すべて成功することを確認済みである。

## 10. Lighthouse検査

Lighthouse CIはGoogle Chromeを使用する。現在のPCには`/usr/bin/google-chrome-stable`が導入済みである。

```bash
google-chrome --version
```

Google Chromeがない環境だけ、Ubuntu用の公式Debianパッケージを導入する。

```bash
curl -fL \
  https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb \
  -o /tmp/google-chrome-stable_current_amd64.deb
sudo apt install -y /tmp/google-chrome-stable_current_amd64.deb
```

検査はモバイル用とデスクトップ用に分かれている。各コマンドは4ページを3回ずつ計測するため、完了まで数分かかることがある。

```bash
npm run test:lighthouse:mobile
npm run test:lighthouse:desktop
```

結果は`lhci_reports/`に出力される。このディレクトリはGit管理対象外である。

## 11. 通常の作業手順

```bash
cd /home/tomokane/.ssh/web/mycraft
git status --short --branch
git pull --ff-only
npm ci
npm run dev
```

ソースや画像を更新した後は、次の順で確認・反映する。

```bash
npm test
npm run test:visual
git status --short
git diff
git add 変更したソースファイル
git add index.html top.html entry.html record.html introduce.html sitemap.xml robots.txt assets
git commit -m "変更内容を記載"
git push
```

`npm run build`後は、`assets/`の古いハッシュ付きファイルが削除され、新しいファイルが生成される場合がある。HTMLと`assets/`は必ず同じコミットに含める。

作業に無関係な未追跡ファイルを混ぜないよう、`git add -A`は避け、`git status --short`で確認したファイルだけを追加する。

## 12. 主なトラブルと対処

### `npm ci`で`EBADENGINE`が出る

Node.jsが古い。`node --version`を確認し、本手順書の「4. Node.js 20の導入」に従って`v20.19.0`以上にする。

### `npm run dev`でポートが使用中と表示される

Viteは通常、別の空きポートを自動選択する。ターミナルに表示された実際のURLを開く。以前の開発サーバーが残っている場合は、起動したターミナルで`Ctrl+C`を押す。

### PlaywrightがChromiumを見つけられない

```bash
npx playwright install --with-deps chromium
```

を再実行する。`node_modules/`を導入する前ではなく、`npm ci`後に実行する。

### 視覚テストで画像差分が出る

すぐに基準画像を更新せず、`playwright-report/`と`test-results/`を確認する。意図した表示変更の場合だけ、レビュー後に基準画像を更新する。

### `git push`で`Permission denied (publickey)`になる

`ssh -T git@github.com`で接続を確認し、GitHubに公開鍵が登録されているか確認する。リポジトリへの書き込み権限も必要である。

## 13. 環境構築完了チェックリスト

```text
[　] WSLがVersion 2で動作している
[　] Ubuntu 24.04 LTSを使用している
[　] Node.jsが20.19.0以上である
[　] npm、Git、Python 3が実行できる
[　] ssh -T git@github.comで認証成功を確認した
[　] originが正しいGitHubリポジトリを指している
[　] npm ciが成功した
[　] npm run devでサイトを表示できた
[　] npm testが成功した
[　] npx playwright install --with-deps chromiumを実行した
[　] npm run test:visualが成功した
[　] 必要に応じてLighthouse検査を実行できる
```

---

以上。
