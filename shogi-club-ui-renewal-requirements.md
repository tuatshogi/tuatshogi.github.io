# 将棋部サイト UIリニューアル 構成要件

作成日: 2026-07-25
対象サイト: 東京農工大学将棋部 公式サイト（tuatshogi.github.io）

---

本ドキュメントは、共有いただいたイメージ画像のUI・情報構成を現行サイトに導入するための構成要件をまとめたものです。ヘッダー・フッターは変更せず、`#content` 内の各ページ構成とデザインを刷新する前提でまとめています。デザインの方向性は以前確認した「和モダン」を踏襲し、モックアップ画像は主にレイアウト・情報構成の参考として扱います。

## 1. 前提・スコープ

- **ヘッダー・フッターは変更しない**: `act.js` の `renderheader()` / `renderFooter()` / `DATA.navitems` / `DATA.footer`、`act.css` の `header.site-header` / `footer.site-footer` 関連スタイルは現行のまま。
- 変更対象は `#content` 内、つまり各ページ（`top.js` / `entry.js` / `record.js` / `introduce.js` および新規ページ）のレイアウトとコンポーネントデザインのみ。
- デザインの方向性は「和モダン」。配色は既存のCSSカスタムプロパティ（`--bg` `--bg-panel` `--accent` `--accent-green` `--gold` `--line` `--font-heading` 等）をベースに拡張し、ヘッダー・フッターとcontentで世界観が分断されないようにする（詳細は3章）。
- イメージ画像はAI生成のモックアップであり、記載されているテキスト（部員名・大会結果の数値・活動日時など）はレイアウト確認用のダミーデータ。実装時は各ページの既存データ、または部内で確認した正確な情報を使用する。

## 2. ページ対応表

| モックアップ | 対応する現行ページ | 対応区分 |
|---|---|---|
| ① HOME | `top.html` / `top.js` | 改修 |
| ② 活動紹介 | `introduce.html` / `introduce.js` | 改修（構成を大幅変更） |
| ③ 大会実績 | `record.html` / `record.js` | 改修（ハイライト追加・表形式化） |
| ④ 部員紹介 | なし | 新規ページ |
| ⑤ ギャラリー | なし | 新規ページ |
| ⑥ お問い合わせ | なし | 新規ページ |
| （モックアップになし） | `entry.html` / `entry.js`（入部案内） | 現状維持（今回のスコープ外） |

## 3. 共通デザインシステム（content内）

### 3.1 配色

モックアップの緑×ゴールドの配色は、既存の `--accent-green`（#2D5A27）・`--gold`（#C8A84E）とほぼ一致している。和モダンの方向性とも合うため、新色を追加せずこの2色をcontent内の主要アクセントカラーとして使う方針を推奨する。

- ボタン・タブのアクティブ状態・リンクホバーなどの主要インタラクション色 → `--accent-green`
- トロフィー・実績バッジ・強調ラベルなどのハイライト色 → `--gold`（既存の `.record-label--gold` と同系統）
- 背景・カードは既存の `--bg` `--bg-panel` および白系をそのまま使用し、モックアップの白背景寄りのトーンに近づける

### 3.2 タイポグラフィ

モックアップ画像はAI生成のため、フォント形状は正確な参照にしない。既存の `--font-heading`（Shippori Mincho）/ `--font-body`（Noto Sans JP）を継続使用する。

### 3.3 新規に必要なコンポーネント

| コンポーネント | 用途 | 備考 |
|---|---|---|
| Hero | トップページ冒頭 | 背景画像＋見出し＋CTAボタン |
| NewsItem | トップページNews一覧 | 日付＋タグバッジ＋タイトル |
| IconNavCard | トップページの4枚カード | 既存 `.body-nav-card` の変形カードを角丸カードに作り替え（4.1参照） |
| InfoRow | 活動紹介ページ | アイコン＋ラベル＋値 |
| HighlightStatCard | 大会実績ページ上部 | 既存 `.record-label--gold` / `--default` の考え方を流用 |
| ResultsTable | 大会実績ページ下部 | 現行の段落形式から `<table>` に変更 |
| MemberCard | 部員紹介ページ | 写真＋氏名＋学年＋得意戦法＋一言 |
| FilterTabs | ギャラリーページ | カテゴリ切り替え |
| PhotoGrid + Lightbox | ギャラリーページ | クリックで拡大表示（現行サイトに前例なし、新規JS実装が必要） |
| ContactForm | お問い合わせページ | 氏名・メール・件名・メッセージ |

## 4. ページ別要件

### 4.1 トップページ（`top.html` / `top.js`）

**現状**: お知らせ1件（段落固定）→ カード3枚（入部案内・大会記録・活動紹介）→ 部室マップ

**変更後**:
1. **Hero セクション**: 見出しコピー＋サブコピー＋CTAボタン「新入部員募集中!」（リンク先: `entry.html`）
2. **News セクション**: 直近3件を一覧表示。1件固定ではなくデータ配列化する
   ```js
   const NEWS = [
     { date: '2026.05.20', tag: 'お知らせ', title: '...', href: '...' },
     // ...
   ];
   ```
   「一覧へ」リンクは、News専用のアーカイブページが今回未設計のためスコープ外とする（7章参照）
3. **アイコンナビカード**: 活動紹介／大会実績（record）／部員紹介／ギャラリーの4枚。入部案内は含まれない点に注意
   - 現行の `.body-nav-card` は将棋の駒を模した変形カード（clip-pathで山形にカット）になっており、エンブレムの駒モチーフと呼応している。角丸カードに作り替える場合も、この駒モチーフをアイコン等の形で残すことを検討する価値がある。現在進めている駒アセット（Blender/Illustrator）をアイコンとして流用できる可能性がある
4. **部室マップ**: 既存セクションをそのまま下部に維持（モックアップには写っていないが、削除する理由がないため維持を推奨）

### 4.2 活動紹介ページ（`introduce.html` / `introduce.js`）

**現状**: 「日頃の活動」（写真1枚＋文章）と「大会・部内戦・レーティング」（写真1枚＋文章）の2セクション構成

**変更後**: 概要文＋InfoRow形式に再構成
- 📅 活動日: 要確認（現行文言は「毎週金曜日」、モックアップの例示は「毎週 月・水・金 17:00〜20:00」で内容が異なるため、実際の活動日程を確認してから記載する）
- 📍 活動場所: 小金井キャンパス サークル棟B棟（`entry.js` の情報と統一）
- 📋 活動内容: 箇条書き（自由対局・感想戦・詰将棋研究・他大学との交流戦 等、既存内容をベースに整理）
- 👥 雰囲気: 既存の「初心者から有段者まで大歓迎」のトーンを踏襲した一文
- 下部に写真ギャラリー（既存2枚 `20260709_180604.jpg` `20260524_191148.jpg` を使用。3枚以上にする場合は追加写真が必要）

「大会・部内戦・レーティング」の内容は、InfoRowの活動内容や大会実績ページとの重複を避けつつ簡潔に統合する。

### 4.3 大会実績ページ（`record.html` / `record.js`）

**現状**: 年度ごとに段落形式で成績を列挙。直近の準優勝のみ `.record-label--gold` で強調

**変更後**:
1. **ハイライトカード（上部・最大3枚）**: 直近の目立つ成績を抽出して表示。既存の `records` 配列から「優勝・準優勝など特筆すべき結果」を優先し、なければ直近の結果を採用するロジックが必要
2. **全記録テーブル（下部）**: 現行の段落形式（`<p>` 羅列）から `<table>`（年度／大会名／結果／詳細の列）に変更
3. 見出し名を「大会記録」のままにするか「大会実績」に変更するかは未決定。ヘッダーのナビラベルは変更しない前提のため、変更する場合はページ内 `<h1>` のみの変更になる（7章参照）

### 4.4 部員紹介ページ（新規: `member.html` / `member.js`）

- 見出し＋リード文
- カードグリッド: 写真・氏名・学年・得意戦法・一言コメント
- 件数が多い場合の「もっと見る」ボタン（初期表示件数は要検討）
- **必要データ（未収集）**: 部員の氏名・学年・得意戦法・コメント・写真。掲載する場合は本人の掲載同意確認が必要

### 4.5 ギャラリーページ（新規: `gallery.html` / `gallery.js`）

- 見出し＋リード文
- フィルタタブ: すべて／対局／イベント／合宿・遠征／その他
- 画像グリッド＋クリックで拡大表示（Lightbox、新規JS実装が必要）
- **必要データ（未収集）**: カテゴリ分類済みの写真素材。現状使える写真は2枚のみのため、ページとして成立させるには追加収集が必要

### 4.6 お問い合わせページ（新規: `contact.html` / `contact.js`）

- 見出し＋リード文＋フォーム（氏名／メールアドレス／件名／メッセージ／送信ボタン）
- **重要な技術的検討事項**: 現サイトは静的サイト（サーバーサイド処理なし）のため、フォームを実際に送信可能にするには次のいずれかが必要
  - 外部フォームバックエンドサービス（Formspree等）の導入
  - `mailto:` によるメールクライアント起動方式への変更
  - その他の代替手段
- `entry.js` では招待リンクを非公開にするなどセキュリティ・スパム対策に配慮した設計方針が既にあるため、不特定多数から送信可能なフォームを新設する方針が既存方針と整合するか、誰が受信・対応するかも合わせて検討が必要

### 4.7 入部案内ページ（`entry.html` / `entry.js`）

モックアップに対応ページがないため、今回は現状維持とする。他ページのデザインが刷新された後、統一感を持たせるために追って改修するかは別途判断（7章参照）。

## 5. レスポンシブ対応

既存サイトは `@media (max-width: 767px)` を基準にモバイル対応している。新規コンポーネントも同様の基準を踏襲する。

- アイコンナビカード（4枚）・部員紹介カードグリッド → モバイルでは1〜2列に折り返す
- 大会実績のハイライトカード（3枚）→ モバイルでは縦積み
- ギャラリーのフィルタタブ → モバイルでは横スクロール可にする、または折り返す

## 6. ナビゲーション・導線

- ヘッダー／フッターのナビ項目は現行どおり（トップ／入部案内／大会記録／活動紹介／X）で変更しない
- 新規3ページ（部員紹介・ギャラリー・お問い合わせ）への導線は、トップページのアイコンナビカードのみ
- そのため、活動紹介ページなど他のページから新規ページへ直接遷移する手段がない点に留意（将来的にヘッダーナビ自体を見直す場合は別タスクとする）

## 7. 未決定事項（実装前に要確認）

1. 大会記録ページの `<h1>` を「大会実績」に変更するか、「大会記録」のまま維持するか
2. トップページNewsの「一覧へ」リンク先（アーカイブページを新設するか、一旦省略するか）
3. お問い合わせフォームの実装方式（外部サービス／mailto／その他）、および公開フォームを新設する方針自体の是非
4. 部員紹介ページの実データ収集方法・掲載同意の取り方
5. ギャラリーページ用の写真収集・カテゴリ分類（担当・期限）
6. 活動紹介ページの活動日の正確な情報（現行「毎週金曜日」／モックアップ例「月・水・金」）
7. 入部案内ページも今回のデザイン刷新に含めるか
8. 新規3ページを将来的にヘッダーナビへ追加するかどうか

## 8. 実装メモ

- 新規ページは現行の読み込み方式（`act.js` の `loadPage()`: `{page}.js` を優先取得して `#content` に描画し、失敗時は `{page}.html` にフォールバック）に合わせ、`{page}.js` を用意する
- 各ページJS冒頭で `document.title` と `meta[name="description"]` を設定する既存の慣習（`top.js` `entry.js` `record.js` `introduce.js` 共通）を新規ページでも踏襲する
- 新規CSSはひとまず既存 `act.css` に追記する方針で問題ないが、量が増える場合はコンポーネント単位でのファイル分割も検討する
- フィルタタブ・Lightboxなど新規のインタラクティブ処理は現行サイトに前例がないため、新規にJSを実装する必要がある
- 画像には既存同様 `alt` 属性を設定し、フォームには `label` を対応付けるなどアクセシビリティ面も既存の水準を維持する
- News・ギャラリー・部員紹介は今後件数が増えていく性質のコンテンツのため、検討中のGitHub Contents APIによるMarkdownファイル駆動の更新方式（別途進行中）と相性が良い可能性がある。ハードコードの配列で始めて後から移行する場合も、データ構造（日付・タグ・タイトル等のフィールド）はこの想定を意識しておくとよい
- 現在進行中のハッシュルーティング／ESモジュール化への移行と本要件は独立して整理しているため、実装時にどちらのルーティング方式を採用するかはあわせて確認する
